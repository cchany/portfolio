# 송찬영 포트폴리오 — 3D 인터랙티브 리디자인 명세서

## 1. 개요

- **베이스**: 기존 `index.html`(단일 파일, 인라인 CSS/JS, Three.js r128 CDN) — 다크 미니멀 + 블루 액센트 + 3D 배경 스피어 + 3D 프로젝트 카로셀은 이미 구현되어 있음
- **목표**: 같은 정체성(미니멀 다크, Apple 느낌)을 유지하면서, 훨씬 정교한 3D 씬과 **스크롤 연동 캐릭터 애니메이션**을 갖춘 고급 인터랙티브 홈페이지로 재구축
- **레퍼런스**: [soijeong.com](https://www.soijeong.com/) — 스크롤 진행률에 따른 인터랙션 흐름만 참고 (캐릭터 자체나 배송 스토리텔링 컨셉은 차용하지 않음)
- **콘텐츠**: 기존 텍스트/이력 그대로 유지, 디자인·인터랙션만 새로 제작
- **마감**: 오늘 (1세션 내 완성 목표 → MVP 우선, 이후 다듬기)

## 2. 디자인 방향

| 항목 | 결정 |
|---|---|
| 무드 | 미니멀 다크 유지 + 고급화 (기존 A안) |
| 배경 | `#000000` / `#0a0a0c` (surface `#111114`) |
| 액센트 컬러 | `#2997ff` (블루, 기존 유지) |
| 폰트 | Sora(디스플레이) / Manrope(본문) / JetBrains Mono(라벨·숫자) — 기존 유지 |
| 레이아웃 | 기존 섹션 구조 유지: Hero → About → Skills → Projects → Experience → Education → Contact |
| 프리미엄 디테일 | 프리로더·커스텀 커서·사운드·라이트 모드 — **이번 범위 제외**, 추후 업데이트 |

## 3. 3D 캐릭터 시스템

- **제작 방식**: Three.js 프리미티브(구·캡슐·토러스 등) 조합으로 만드는 **프로시저럴 로우폴리 캐릭터** — 외부 3D 모델(glb) 없이 코드로 완결
  - 기존 배경의 메탈릭 스피어 3개(`specs` 배열, `index.html` 스크립트 참고)를 확장해 몸통/머리/눈 등 파츠로 구성한 마스코트 형태로 재구성
  - `MeshPhysicalMaterial`(clearcoat) 유지 → 기존 씬과 재질 톤 통일
- **등장 범위 (C안 — 포인트형)**: Hero 섹션에만 메인으로 등장, 이후 섹션에서는 작은 포인트 요소로 가끔만 노출(예: 스크롤 진행에 따라 화면 한쪽 구석에 작게 떠 있다가 특정 섹션 진입 시 살짝 반응하는 정도). 상시 동행하지 않음 → 구현 부담 최소화
- **스크롤 연동**: GSAP `ScrollTrigger` 기준 스크롤 진행률(0~1)을 캐릭터의 회전/부유(float)/카메라 거리에 매핑. Hero 구간에서는 마우스 패럴랙스 + 스크롤 진행에 따른 포즈 전환(예: 정면 → 살짝 옆으로 회전하며 퇴장)
- **향후 실사 3D 캐릭터로 교체하고 싶을 경우 (이번 범위 아님, 참고용)**:
  1. GPT(이미지 생성)로 캐릭터 컨셉 아트 제작
  2. Meshy AI / Tripo AI / Rodin 같은 image-to-3D 툴로 `.glb` 메시 생성 (또는 사람 형태면 Ready Player Me)
  3. 생성된 `.glb`를 주면 로더·스켈레톤 애니메이션·스크롤 연동 통합 코드는 이어서 작업 가능

## 4. 기술 스택

- **빌드**: Vite (B안 — 모듈화된 소스, npm 패키지 관리)
- **3D**: `three` (npm, 최신 버전으로 업그레이드 — 기존 r128 CDN → deprecated API 정리)
- **애니메이션/스크롤**: `gsap` + `ScrollTrigger`
- **구조 제안**:
  ```
  portfolio/
    src/
      main.js
      scene/
        background.js      // 배경 3D 씬 (스피어 + 캐릭터)
        character.js        // 프로시저럴 캐릭터 빌더 + 포즈/애니메이션
        avatar.js            // About 섹션 미니 아바타 씬
        carousel.js          // 프로젝트 3D 카로셀
      sections/
        (섹션별 텍스트/데이터 — 기존 콘텐츠 그대로 이관)
      styles/
        main.css
    index.html
    vite.config.js
    package.json
  ```
- **디바이스 대응 (B안)**: 모바일 포함 전 디바이스 동일한 Three.js 씬 렌더링. 별도 저사양 폴백 없음 — `devicePixelRatio` 캡(≤2), `prefers-reduced-motion` 대응만 기존처럼 유지
- **배포**: GitHub Pages 그대로 유지
  - Vite 빌드 산출물(`dist/`)을 GitHub Actions로 빌드 후 `gh-pages` 브랜치(또는 Pages 소스)로 배포
  - 워크플로: push to `main` → `vite build` → `dist/` 배포

## 5. 섹션별 인터랙션 스펙

| 섹션 | 유지 콘텐츠 | 3D/인터랙션 강화 포인트 |
|---|---|---|
| Hero | 이름/타이틀/태그라인/CTA | 프로시저럴 캐릭터 메인 등장, 마우스 패럴랙스, 스크롤 진입 시 캐릭터 리액션 |
| About | 프로필 텍스트, 학력 요약 | 기존 미니 아바타 씬 유지·고도화 (재질/조명 업그레이드) |
| Skills | Python/Backend/Frontend/Database 4카드 | 기존 tilt 카드 유지, 재질감·그림자 보강 |
| Projects | LOL 프로젝트 1개 + SOON 5슬롯 | 기존 3D 카로셀 유지, 조명/그림자/전환 곡선 고급화 |
| Experience / Education | 기존 타임라인 | 스크롤 진행률에 따라 캐릭터가 타임라인 옆에 작게 나타나는 포인트 연출(선택적) |
| Contact | 이메일/GitHub 링크 | 캐릭터 마지막 인사 포즈로 간단히 마무리 |

## 6. 작업 계획 (오늘 완료 목표)

1. **Vite 프로젝트 셋업** — `npm create vite`, three/gsap 설치, 기존 `index.html`의 마크업·콘텐츠를 모듈 구조로 이관
2. **베이스 씬 이식** — 배경 스피어 씬 + 아바타 씬 + 프로젝트 카로셀을 최신 three.js API로 마이그레이션 (r128 → 최신, deprecated 옵션 정리)
3. **프로시저럴 캐릭터 제작** — 파츠 조합 마스코트 빌드, Hero 배치
4. **스크롤 연동 구현** — GSAP ScrollTrigger로 스크롤 진행률 → 캐릭터/카메라/배경 트랜스폼 매핑
5. **비주얼 고급화 패스** — 라이팅·재질·그림자·색 보정(톤매핑) 다듬기, 각 섹션 등장 애니메이션 정리
6. **반응형 점검** — 모바일 포함 전 디바이스에서 프레임 확인
7. **배포 파이프라인** — GitHub Actions로 Vite 빌드 → GitHub Pages 배포 워크플로 추가
8. **최종 검수** — 실제 브라우저로 전체 스크롤 플로우 확인 후 전달

## 7. 이번 범위에서 제외 (다음 라운드 후보)

- 실사 3D 캐릭터(glb) 교체
- 프리로더 / 커스텀 커서 / 사운드 디자인 / 라이트 모드
- 모바일 전용 경량 폴백
- 이력서 다운로드, 다국어 버전
